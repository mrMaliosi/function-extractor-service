version: '3.8'

services:
  extractor-service:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
    volumes:
      - ./src:/app/src
    command: uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  logs: