# Миграция на uv

Этот документ описывает изменения, внесенные при переводе проекта на пакетный менеджер `uv`.

## Что изменилось

### Новые файлы

- **pyproject.toml** - основной файл конфигурации проекта и зависимостей для uv
- **.gitignore** - обновлен для игнорирования файлов uv (.uv/, uv.lock)
- **MIGRATION.md** - этот файл с описанием миграции

### Обновленные файлы

- **Dockerfile** - теперь использует uv вместо pip для установки зависимостей
- **docker-compose.yml** - обновлена команда запуска для использования uv
- **README.md** - добавлена полная инструкция по работе с uv

### Изменения в зависимостях

1. **clang → libclang**: Заменен пакет `clang==15.0` на `libclang>=16.0.0`, так как правильное название пакета на PyPI - `libclang` (импортируется как `clang.cindex`)

2. Все зависимости теперь указаны в `pyproject.toml` вместо `requirements.txt`

3. Версии зависимостей используют диапазоны (`>=`) вместо фиксированных версий для большей гибкости

## Как начать работу после миграции

1. Установите uv (если еще не установлен):
   ```bash
   curl -LsSf https://astral.sh/uv/install.sh | sh
   ```

2. Установите зависимости:
   ```bash
   uv sync
   ```

3. Запустите проект:
   ```bash
   uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
   ```

## Обратная совместимость

Старый файл `requirements.txt` сохранен для справки, но не рекомендуется к использованию. Все команды теперь должны использовать `uv` и `pyproject.toml`.

## Преимущества uv

- ⚡ **Быстрота**: uv написан на Rust и работает значительно быстрее pip
- 🔒 **Надежность**: автоматическая фиксация версий в `uv.lock`
- 🎯 **Универсальность**: объединяет функциональность pip, virtualenv, pip-tools
- 📦 **Современность**: использует стандарты PEP 621 (pyproject.toml)

## Дополнительные команды

```bash
# Добавить новую зависимость
uv add package-name

# Добавить зависимость для разработки
uv add --group dev package-name

# Удалить зависимость
uv remove package-name

# Обновить все зависимости
uv lock --upgrade
uv sync

# Запустить команду в виртуальном окружении
uv run python script.py

# Активировать виртуальное окружение вручную
source .venv/bin/activate  # Linux/macOS
.venv\Scripts\activate     # Windows
```
