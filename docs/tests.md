# [TODO]: Python

> Несколько функций в одном файле: файл, где идут 2–3 def подряд (включая приватную _helper, async def, декораторы) и проверка, что извлекаются/обрабатываются все, а не только первая.
> Смешанный файл class + def: в одном модуле есть класс с методами и отдельная функция на верхнем уровне; важно убедиться, что методы не принимаются за top-level функции (или наоборот — как у вас задумано).